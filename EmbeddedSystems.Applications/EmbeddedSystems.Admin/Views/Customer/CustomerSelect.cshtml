@model IEnumerable<EmbeddedSystems.DomainModel.Customer>

@this.Html.DropDownList("customerId", this.Model.Select(c => new SelectListItem() { Text = c.Name, Value = c.Id.ToString() }), new { @class="form-control" })